diff -Nrub "build\\binary\\gpgme\\work\\include.orig/QGpgME/dataprovider.h" "build\\binary\\gpgme\\work\\include/QGpgME/dataprovider.h"
--- "build\\binary\\gpgme\\work\\include.orig/QGpgME/dataprovider.h"	2018-04-26 14:06:59.000000000 -0700
+++ "build\\binary\\gpgme\\work\\include/QGpgME/dataprovider.h"	2018-04-27 05:38:24.574359600 -0700
@@ -55,8 +55,8 @@
     {
         return true;
     }
-    ssize_t read(void *buffer, size_t bufSize);
-    ssize_t write(const void *buffer, size_t bufSize);
+    int read(void *buffer, size_t bufSize);
+    int write(const void *buffer, size_t bufSize);
     off_t seek(off_t offset, int whence);
     void release();
 
@@ -80,8 +80,8 @@
     // these shall only be accessed through the dataprovider
     // interface, where they're public:
     bool isSupported(Operation) const;
-    ssize_t read(void *buffer, size_t bufSize);
-    ssize_t write(const void *buffer, size_t bufSize);
+    int read(void *buffer, size_t bufSize);
+    int write(const void *buffer, size_t bufSize);
     off_t seek(off_t offset, int whence);
     void release();
 
diff -Nrub "build\\binary\\gpgme\\work\\include.orig/QGpgME/encryptjob.h" "build\\binary\\gpgme\\work\\include/QGpgME/encryptjob.h"
--- "build\\binary\\gpgme\\work\\include.orig/QGpgME/encryptjob.h"	2018-04-26 14:06:59.000000000 -0700
+++ "build\\binary\\gpgme\\work\\include/QGpgME/encryptjob.h"	2018-04-27 05:38:50.432303300 -0700
@@ -90,7 +90,7 @@
        not be performed, but full validity assumed for all keys
        without further checks.
     */
-    virtual QGPGME_DEPRECATED_EXPORT GpgME::Error start(const std::vector<GpgME::Key> &recipients,
+    virtual GpgME::Error start(const std::vector<GpgME::Key> &recipients,
             const QByteArray &plainText, bool alwaysTrust = false) = 0;
 
     /*!
diff -Nrub "build\\binary\\gpgme\\work\\include.orig/QGpgME/signjob.h" "build\\binary\\gpgme\\work\\include/QGpgME/signjob.h"
--- "build\\binary\\gpgme\\work\\include.orig/QGpgME/signjob.h"	2018-04-26 14:06:59.000000000 -0700
+++ "build\\binary\\gpgme\\work\\include/QGpgME/signjob.h"	2018-04-27 05:39:02.190278600 -0700
@@ -85,7 +85,7 @@
        Starts the signing operation. \a signers is the list of keys to
        sign \a plainText with. Empty (null) keys are ignored.
     */
-    virtual QGPGME_DEPRECATED_EXPORT GpgME::Error start(const std::vector<GpgME::Key> &signers,
+    virtual GpgME::Error start(const std::vector<GpgME::Key> &signers,
             const QByteArray &plainText,
             GpgME::SignatureMode mode) = 0;
 
diff -Nrub "build\\binary\\gpgme\\work\\include.orig/gpgme++/data.h" "build\\binary\\gpgme\\work\\include/gpgme++/data.h"
--- "build\\binary\\gpgme\\work\\include.orig/gpgme++/data.h"	2018-04-26 14:06:59.000000000 -0700
+++ "build\\binary\\gpgme\\work\\include/gpgme++/data.h"	2018-04-27 05:37:52.287586300 -0700
@@ -106,8 +106,8 @@
     char *fileName() const;
     Error setFileName(const char *name);
 
-    ssize_t read(void *buffer, size_t length);
-    ssize_t write(const void *buffer, size_t length);
+    int read(void *buffer, size_t length);
+    int write(const void *buffer, size_t length);
     off_t seek(off_t offset, int whence);
 
     /** Try to parse the data to a key object using the
diff -Nrub "build\\binary\\gpgme\\work\\include.orig/gpgme++/interfaces/dataprovider.h" "build\\binary\\gpgme\\work\\include/gpgme++/interfaces/dataprovider.h"
--- "build\\binary\\gpgme\\work\\include.orig/gpgme++/interfaces/dataprovider.h"	2018-04-26 14:06:59.000000000 -0700
+++ "build\\binary\\gpgme\\work\\include/gpgme++/interfaces/dataprovider.h"	2018-04-27 05:37:18.353863300 -0700
@@ -44,8 +44,8 @@
     };
     virtual bool isSupported(Operation op) const = 0;
 
-    virtual ssize_t read(void   *buffer, size_t bufSize) = 0;
-    virtual ssize_t write(const void *buffer, size_t bufSize) = 0;
+    virtual int read(void   *buffer, size_t bufSize) = 0;
+    virtual int write(const void *buffer, size_t bufSize) = 0;
     virtual off_t seek(off_t offset, int whence) = 0;
     virtual void release() = 0;
 };
diff -Nrub "build\\binary\\gpgme\\work\\include.orig/gpgme++/key.h" "build\\binary\\gpgme\\work\\include/gpgme++/key.h"
--- "build\\binary\\gpgme\\work\\include.orig/gpgme++/key.h"	2018-04-26 14:06:59.000000000 -0700
+++ "build\\binary\\gpgme\\work\\include/gpgme++/key.h"	2018-04-27 05:36:52.839210200 -0700
@@ -30,7 +30,7 @@
 #include "gpgmefw.h"
 
 #include <memory>
-#include <sys/time.h>
+#include <time.h>
 
 #include <vector>
 #include <algorithm>
